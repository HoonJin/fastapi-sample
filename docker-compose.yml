version: '3'

services:
  viole:
    build:
      context: .
      dockerfile: ./compose/Dockerfile-dev
    environment:
      - ENV=dev
    ports:
      - "8000:8000"
    command:
      - bash
      - -c
      - |
        uvicorn main:app --host 0.0.0.0 --reload
    volumes:
      - ./:/app/
